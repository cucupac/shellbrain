[pytest]
testpaths =
    tests
markers =
    real_embedding: runs tests that require sentence-transformers model loading
    docker: runs tests that require Docker and Docker Compose
    persistence: runs destructive persistence tests against isolated Docker-backed PostgreSQL instances
