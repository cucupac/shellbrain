Ports are behavior protocols the core requires from outside systems. They are grouped by the outside adapter category that implements them, such as `db`, `embeddings`, `host_apps`, `local_state`, `reporting`, and `system`; core depends only on these abstractions.
