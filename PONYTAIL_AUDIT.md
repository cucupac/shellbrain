# Ponytail repository audit (pass 2)

Date: 2026-09-01. A first pass landed in commit 3054d4c (1,349 net lines removed). This pass re-audits the whole tracked tree with five parallel subsystem agents.

## Scope map

Python 3.11 package (setuptools), pytest, SQLAlchemy + Alembic on PostgreSQL/pgvector, Pydantic, PyYAML, sentence-transformers. Tracked tree is about 101,800 lines including tests, docs, and assets. Untracked working files (`architectures/`, `insights/`, `research/`, `writing/`, `output/`, root HTML explainers, `next_steps.md`, `release_notes.md`) are outside the audit.

| Agent | Scope (mutually exclusive) | Approx. lines |
| --- | --- | --- |
| 1. Core domain | `app/core/{entities,policies,ports,errors.py}`, `app/core/use_cases/{memories,concepts,episodes,scenarios,snapshots,admin,metrics,build_guidance.py,plan_execution.py}`; tests `tests/operations/{memories,concepts,episodes,scenarios,snapshots,events,memory_domain,protection,librarian,evidence}` | 10,000 prod |
| 2. Retrieval and knowledge | `app/core/use_cases/{retrieval,knowledge_builder}`, `app/infrastructure/embeddings`; tests `tests/operations/{read,recall,retrieval,knowledge_builder}` | 6,500 prod |
| 3. Persistence and schema | `app/infrastructure/db`, `migrations`, `alembic.ini`; tests `tests/operations/{create,update,persistence}`, `tests/infrastructure` | 14,600 prod/schema |
| 4. Runtime and host integration | `app/infrastructure/{host_apps,local_state,process,system,telemetry}`, `app/entrypoints`, `app/startup`, `app/settings`; tests `tests/operations/{identity,session_state,guidance,recovery,resilience,telemetry}` | 17,500 prod |
| 5. Project delivery | `pyproject.toml`, `requirements.txt`, `pytest.ini`, Docker files, `.github`, `.githooks`, `scripts`, `onboarding_assets`, `docs`, `README.md`; tests `tests/{config,_shared,visualization,conftest.py,__init__.py}` | 6,200 delivery + 7,400 test support |

## Agent 1 — Core domain

shrink: Collapse the five near-identical evidence builders (`_operation_evidence`, `_sync_evidence`, `_read_evidence`, `_write_evidence`, `_guidance_evidence`) into list comprehensions, drop the never-varied `inclusive` parameter and the `direction` branch of `_has_prior_time` (callers pass only the defaults plus one `direction="after"`), drop the `_thread_times` sort that a linear `any()` scan never needs, and replace the `_REAL_DATETIME` alias with `datetime` (the only monkeypatch targets `app.startup.analytics`, not this module). `any(t > created_at for t in times)` / `any(t < created_at ...)` and plain comprehensions. [app/core/use_cases/admin/generate_analytics_report.py]
shrink: Replace the four copy-pasted row-filter blocks in `_build_window_report` (overall, by_repo, by_host, by_repo_host each re-filter operation/read/write/checkpoint rows) with one `_slice(rows, predicate)` helper applied to the four row lists. Same metrics, one filter site. [app/core/use_cases/metrics/analyze_agent_behavior.py]
delete: Remove the "reserved future" `GraphPatch` entity, `GraphPatchStatus` enum, and `IConceptsRepo.create_graph_patch`. No caller anywhere (`create_graph_patch` appears only in the port and the repo impl; the inner-agent prompt says "Do not use graph_patches"); the repo impl and `graph_patches` table become deletable for agent 3. [app/core/entities/concepts.py, app/core/ports/db/concept_repositories.py]
yagni: Remove the `verify_backup` use case and the `BackupVerificationResult` entity. The use case discards the manifest to return `verified=True`, then `app/startup/backup.py` re-resolves the manifest by id; call `logical_backup.verify_backup(**kwargs)` directly. `BackupVerificationResult` has no other reference. [app/core/use_cases/admin/verify_backup.py, app/core/entities/backups.py]
yagni: Remove `SyncEpisodeRequest.from_raw_events`. Pydantic already validates `Mapping` items into `NormalizedEpisodeEvent` and accepts instances for `normalized_events: tuple[NormalizedEpisodeEvent, ...]`; the three callers (sync_discovered_host_session, snapshot handler, events handler) can construct `SyncEpisodeRequest(...)` directly. [app/core/use_cases/episodes/sync_episode/request.py]
yagni: Remove the `CodeDeltaContextStatus` enum, the constant `status` fields on `AvailableCodeDeltaContext`/`UnavailableCodeDeltaContext`, and their two `__post_init__` guards that only reject a value nothing sets. Hardcode `"available"`/`"unavailable"` in `to_response_data` (payload unchanged); no external reference to `CodeDeltaContextStatus`. Also inline the `_unavailable(reason)` one-liner as `UnavailableCodeDeltaContext(reason=...)`. [app/core/entities/snapshots.py, app/core/use_cases/snapshots/code_delta_context/execute.py]
delete: Remove the `data` properties on `CreateMemoryResult`, `UpdateMemoryResult`, and `BatchUpdateMemoryResult`. `ok_envelope` prefers `to_response_data` (which all three define), and the CLI handlers read `core_result.planned_effects` directly; `planned_side_effects` is never read from `.data`. [app/core/use_cases/memories/add/result.py, app/core/use_cases/memories/update/result.py]
yagni: Remove `SessionTransfer` and `IEpisodesRepo.append_transfer`. No caller in app or tests (the constraint test inserts into the table directly); the repo impl follows for agent 3. [app/core/entities/episodes.py, app/core/ports/db/episode_repositories.py]
yagni: Delete `app/core/policies/episodes/event_content.py` and `NormalizedEpisodeEvent.to_content_dict`. Both wrap one call each; use `json.dumps(event.model_dump(mode="python"), sort_keys=True, separators=(",", ":"), ensure_ascii=False)` at the single call site and `event.model_dump(mode="python")` in `_count_normalized_events`. [app/core/policies/episodes/event_content.py, app/core/use_cases/episodes/sync_episode/request.py]
yagni: Delete `containment_checks.py`; its only function guards `predicate == CONTAINS` then forwards to `validate_no_contains_cycle`, and it has one caller. Inline the three lines into `_add_relation`. [app/core/use_cases/concepts/containment_checks.py]
yagni: Remove the `create_backup` use case; it calls the injected adapter and re-checks that `BackupManifest.backup_id` (a required `str` field) is non-empty. `app/startup/backup.py` calls `logical_backup.create_backup(**kwargs)` directly. [app/core/use_cases/admin/create_backup.py]
shrink: Consolidate the nine identical `_StrictModel(BaseModel)` classes plus the duplicated `_normalize_required_string` (memories/add, concepts/add) and `_normalize_optional_text` (memories/update, concepts/update) into one shared core request-base module. Same validators, one definition. [app/core/use_cases/memories/add/request.py, app/core/use_cases/concepts/add/request.py, app/core/use_cases/concepts/update/request.py, app/core/use_cases/episodes/events/request.py, app/core/use_cases/episodes/sync_episode/request.py, app/core/use_cases/scenarios/record/request.py, app/core/ports/host_apps/inner_agents.py]
yagni: Remove the eight constant-injection fields from `InitializeRuntimePorts` (`bootstrap_state_ready/repair_needed/provisioning`, `runtime_mode_managed_local/external_postgres`) and `DiagnoseRuntimePorts` (`runtime_mode_managed_local/external_postgres`). Startup passes the `BOOTSTRAP_STATE_*`/`RUNTIME_MODE_*` constants from `app.core.entities.machine_config`, which core may import directly; keep only the infra-owned `identity_strength_weak_local`. [app/core/use_cases/admin/initialize_runtime.py, app/core/use_cases/admin/diagnose_runtime.py]
shrink: Derive the lifecycle tables in `ontology_semantics.py` from `MemoryLifecycleStatus` instead of restating them: `LIFECYCLE_RETRIEVAL_MULTIPLIERS` duplicates `MemoryLifecycleStatus.retrieval_multiplier`, `POSITIVE_LIFECYCLE_STATUSES` duplicates `DEFAULT_RETRIEVABLE_MEMORY_STATUS_VALUES`, and the six `*_STATUS` strings duplicate the enum values. `{s.value: s.retrieval_multiplier for s in MemoryLifecycleStatus}`. [app/core/policies/retrieval/ontology_semantics.py]
delete: Remove the duplicate-slug branch in `add_concepts`. `ConceptAddRequest._validate_unique_slugs` already rejects requests whose normalized slugs collide (identical normalizer), so the `seen_slugs` check and its `DomainValidationError` are unreachable; no test references "duplicate slug". [app/core/use_cases/concepts/add/execute.py]
shrink: Replace `_count_normalized_events` (hand-rolled per-source counters plus a dict the result then `int()`-casts field by field) with `Counter(event.source for event in events)` and a `Counter` over tool names. Same `SyncEpisodeResult`. [app/core/use_cases/episodes/sync_episode/sync_episode.py]
yagni: Remove `MemoryAddLinks.related_memory_ids` and its validator that exists only to reject any value. `extra="forbid"` already rejects the key at the same `memory.links.related_memory_ids` location; the one test asserts the field path only. [app/core/use_cases/memories/add/request.py]
stdlib: Replace `_unique_terms` with `tuple(dict.fromkeys(terms))`, drop the `raw_terms`/`informative_terms` fields on `LexicalQuery` (only `terms` and `uses_stopword_fallback` are read outside the builder), and drop the never-passed `k1`/`b` parameters of `score_documents`. [app/core/policies/retrieval/lexical_query.py, app/core/policies/retrieval/bm25.py]
shrink: Make `validate_evidence_visibility` call `require_visible_memory` instead of repeating the memory lookup and visibility check with different message text. No test references the "Evidence memory" messages. [app/core/use_cases/concepts/reference_checks.py]
yagni: Remove port methods with no core or entrypoint caller: `IEpisodesRepo.list_event_keys` and `close_episode` (no caller at all), `IEvidenceRepo.resolve_evidence` (only its own repo test), `ISessionStateStore.list` (only the file store's own `gc` and one cleanup test), and `IEpisodesRepo.create_episode` (only test fixtures; they can use `get_or_create_episode_for_thread`). Repo impls follow for agents 3 and 4. [app/core/ports/db/episode_repositories.py, app/core/ports/db/memory_repositories.py, app/core/ports/local_state/session_state_store.py]
shrink: Replace `MATURE_MEMORY_KINDS`, which lists every `MemoryKind` member, with `MATURE_MEMORY_KIND_VALUES = tuple(kind.value for kind in MemoryKind)`. Same three consumers. [app/core/entities/memories.py]
yagni: Remove `BackfillModelUsageRequest`, a dataclass wrapping one `sessions` tuple with one caller; pass `tuple[LinkedModelUsageSession, ...]` to `backfill_model_usage`. [app/core/use_cases/admin/backfill_model_usage/request.py]
shrink: Inline `build_canonical_caller_id` into `CallerIdentity.__post_init__`; it is its only caller. [app/core/entities/identity.py]
stdlib: Replace the manual dict-count loops in `_evidence_counts` and `_status_rollup` with `collections.Counter`. [app/core/use_cases/concepts/views.py]
shrink: Remove `make_side_effect` and pass `EffectType` members directly: `PlannedEffect(EffectType.MEMORY_CREATE, params)` instead of string literals re-parsed through `EffectType(effect_type)`. [app/core/use_cases/memories/effect_plan.py, app/core/policies/memories/add_plan.py, app/core/policies/memories/update_plan.py]
shrink: Remove `MachineConfig.machine_instance_id`, an alias property for `instance_id`; the six callers use `.instance_id`. [app/core/entities/machine_config.py]
yagni: Remove the accepted-but-dropped `rationale` fields on `MemoryAddBody`, `MemoryAddAssociationLink`, `FactUpdateLinkUpdate`, and `AssociationLinkUpdate`. Neither planner reads them (only lifecycle and utility_vote rationale reach storage), and no test, prompt, or onboarding asset sends them. [app/core/use_cases/memories/add/request.py, app/core/use_cases/memories/update/request.py]
delete: Remove `GuidanceDecision.setup_hint` and its `to_payload` branch; no constructor sets it. [app/core/entities/guidance.py]
delete: Remove `ErrorCode.HOST_IDENTITY_UNAVAILABLE`, `TRANSCRIPT_SOURCE_NOT_FOUND`, and `INNER_AGENT_ERROR`; never raised or matched (no reference to the names or string values outside the enum). [app/core/errors.py]
delete: Remove the unused `InvocationId` and `UtilityVote` NewTypes. [app/core/entities/ids.py]
net: -460 lines, -0 deps possible.

## Agent 2 — Retrieval and knowledge

delete: Remove `execute_recall_memory` and its module; the only consumer is `app/entrypoints/cli/handlers/internal_agent/retrieval/execution.py` (grep of app/, tests/, docs/, onboarding_assets/, scripts/, pyproject.toml finds no other import, no entry point, no doc mention). Import `execute_build_context` there directly; this removes the only edge that forced the lazy `__getattr__` in `recall/__init__.py` (recall.execute -> build_context package -> deterministic_graph_recall -> recall.request), so both `recall/__init__.py` and `build_context/__init__.py` become plain imports. Verdict: internal-only alias, safe to delete. [app/core/use_cases/retrieval/recall/execute.py, app/core/use_cases/retrieval/recall/__init__.py, app/core/use_cases/retrieval/build_context/__init__.py]
shrink: The twelve provider run fields (`write_count` ... `error_message`) are spelled out four times per file in `knowledge_build_runs.complete(...)` and the result constructor. Define `RUN_RESULT_FIELDS` once next to `KnowledgeBuildRun` and spread `provider_result.model_dump(include=RUN_RESULT_FIELDS)`; `BuildKnowledgeAgentResult`, `KnowledgeBuildRun`, `complete()`, and both result dataclasses already share the exact field names. [app/core/use_cases/knowledge_builder/build_knowledge/execute.py, app/core/use_cases/knowledge_builder/teach_knowledge/execute.py]
shrink: `_fresh_running_run` and the 20-line stale-run `TIMEOUT` completion loop are duplicated verbatim between the two knowledge-builder executors. One `timeout_stale_runs(uow, running_runs, now, stale_seconds, message)` helper in the knowledge_builder package replaces both copies. [app/core/use_cases/knowledge_builder/teach_knowledge/execute.py, app/core/use_cases/knowledge_builder/build_knowledge/execute.py]
yagni: `TeachKnowledgeResult` re-declares all 18 `BuildKnowledgeResult` fields and its `to_response_data` already nests them under `"build_knowledge"`. Hold `build: BuildKnowledgeResult` plus the three teaching fields and delegate; `_skipped_result` in teach shrinks to a `BuildKnowledgeResult` call. Tests read `result.status`/`.run_id`/`.write_count`/`.error_code` at ~8 sites and move to `result.build.*`. [app/core/use_cases/knowledge_builder/teach_knowledge/result.py]
yagni: `_autonomous_fallback_graph_result_with_uow` forwards every argument unchanged to `_deterministic_graph_result_with_uow` with `prior_inner_agent_result=`. Call the target directly from `execute_build_context`. [app/core/use_cases/retrieval/build_context/execute.py]
shrink: The 10-line memory-candidate default dict (`memory`, `score`, `matched_lanes`, `lane_ranks`, `concept_refs`, `link_roles`, `why`) is repeated three times in `_run_memory_lane`, `_expand_structural_memory_relations`, and `_traverse_selected_concepts`. One `_new_candidate(memory)` factory. [app/core/use_cases/retrieval/deterministic_graph_recall.py]
shrink: Five one-line delegating wrappers with one to three callers each: `_active` (= `is_active_lifecycle`), `_concept_freshness_multiplier`, `_freshness_payload`, `_concept_currentness_payload`, `_memory_currentness_payload`. Call the policy functions at the call sites. [app/core/use_cases/retrieval/deterministic_graph_recall.py]
shrink: Query-vector resolution (`embed_query`, empty-vector `ValueError`, `model_name`) exists three times: `read/execute._resolve_query_embedding`, `deterministic_graph_recall._query_embedding` (with a needless `getattr(uow, "vector_search", None)`; `IUnitOfWork` declares the attribute and the test `_FakeUow` sets it), and inline in `retrieve_seeds` lines 42-48. Keep one `resolve_query_embedding(vector_search, query)` in `seed_retrieval.py`, have `_run_memory_lane` pass `query_vector`/`query_model`, and drop the `vector_search` parameter from `retrieve_seeds` and `build_context_pack` (the pipeline always receives a resolved vector; the two direct test callers pass `vector_search=None`). [app/core/use_cases/retrieval/seed_retrieval.py, app/core/use_cases/retrieval/read/execute.py, app/core/use_cases/retrieval/deterministic_graph_recall.py, app/core/use_cases/retrieval/context_pack_pipeline.py]
delete: Remove `_run_status` in both executors; `BuildKnowledgeAgentResult.status` is typed `KnowledgeBuildRunStatus` on a pydantic model without `use_enum_values`, so the value is already the enum and the `str` coercion branch is unreachable. Use `provider_result.status`. [app/core/use_cases/knowledge_builder/build_knowledge/execute.py, app/core/use_cases/knowledge_builder/teach_knowledge/execute.py]
yagni: Revisit the retained read seam: replace `read/__init__.__getattr__` and the `_build_context_pack` re-lookup hook with `from app.core.use_cases.retrieval import context_pack_pipeline` and a `context_pack_pipeline.build_context_pack(...)` call. The two monkeypatching tests (`test_concept_aware_read.py:588`, `test_read_execution_semantic_wiring.py:151`) patch `app.core.use_cases.retrieval.context_pack_pipeline.build_context_pack` instead; the seam survives, the indirection does not. No import cycle: `read_concepts` -> `read.request` is a submodule import that does not need package attributes. [app/core/use_cases/retrieval/read/__init__.py, app/core/use_cases/retrieval/read/execute.py]
shrink: `_hydrate_pack_items` tests the same four keys three times and the final "Incomplete hydrated memory" branch cannot fire after four `setdefault` calls. `_DISPLAY_FIELDS = ("kind", "text", "created_at", "status")`, one `all(f in item for f in _DISPLAY_FIELDS)` predicate, and drop the dead post-check. No test references the error path. [app/core/use_cases/retrieval/context_pack_pipeline.py]
shrink: Identical `_iso(datetime | None) -> str | None` in three scope modules (a fourth lives in `concepts/views.py`). One shared `iso_timestamp` in `app/core/entities` imported by all. [app/core/use_cases/retrieval/context_pack_pipeline.py, app/core/use_cases/retrieval/read_concepts.py, app/core/use_cases/retrieval/deterministic_graph_recall.py]
stdlib: `_deterministic_graph_result_with_uow` duplicates an 8-kwarg call for the `uow` and `uow_factory` branches. `with (nullcontext(uow) if uow is not None else uow_factory()) as graph_uow:` and one call. [app/core/use_cases/retrieval/build_context/execute.py]
shrink: `_deterministic_graph_result` builds `RecallMemoryResult` three times with the same `telemetry={"candidate_pack", "source_items", "inner_agent": _with_graph_counts(...)}` block. One local `_graph_result(brief, fallback_reason, inner_agent_result)` closure. [app/core/use_cases/retrieval/build_context/execute.py]
delete: The empty-recall brief literal in `deterministic_brief_from_graph_pack` (lines 211-221) duplicates `_no_context_brief` in `build_context/execute.py`. Export `no_context_brief()` from `deterministic_graph_recall` and import it in build_context. [app/core/use_cases/retrieval/deterministic_graph_recall.py, app/core/use_cases/retrieval/build_context/execute.py]
yagni: `_DEFAULT_SETTINGS` is only reached when `build_context_settings` is `None`; production always injects `dependencies.build_context_settings` (`internal_agent/retrieval/execution.py:41`). Make the parameter required and give the three defaulting tests in `test_build_context.py` a `_synthesis_settings()` helper beside the two they already have. [app/core/use_cases/retrieval/build_context/execute.py]
delete: `synthesis_pack_from_graph_pack` only returns `_project_synthesis_pack(pack=pack)`. Rename the private function to the public name and keep the docstring. [app/core/use_cases/retrieval/deterministic_graph_recall.py]
delete: Remove `BuildKnowledgeResult.data` and `TeachKnowledgeResult.data`; both only return `self.to_response_data()`, `ok_envelope` checks `to_response_data` first, `app/startup/knowledge_builder.py` and `working_agent/teach.py` call `to_response_data()`, and no test in `tests/operations/knowledge_builder` reads `.data`. [app/core/use_cases/knowledge_builder/build_knowledge/result.py, app/core/use_cases/knowledge_builder/teach_knowledge/result.py]
shrink: In `read_concepts.py`: `_freshness_multiplier` (one caller) and `_concept_expand` (one caller) inline to one expression each; `AVAILABLE_FACETS[:MAX_EXPAND_HANDLES]` slices a 5-tuple by 5, drop the constant; `_append_rank_reason`'s duplicate check is dead because `fuse_with_rrf` yields one row per `concept_id`, so `_append_retrieval_reasons` runs once per candidate, append directly. [app/core/use_cases/retrieval/read_concepts.py]
shrink: Inline the single-use `_keyword_candidate_limit` and `_concept_keyword_candidate_limit` as `max(limit * 25, 200)` / `max(limit * 10, 100)` at their one call site each. [app/core/use_cases/retrieval/seed_retrieval.py, app/core/use_cases/retrieval/concept_seed_retrieval.py]
shrink: `_bundle_lifecycle_statuses` is duplicated verbatim in `read_concepts.py` and `deterministic_graph_recall.py`. Move it beside `concept_bundle_retrieval_multiplier` in `ontology_semantics.py` and import. [app/core/use_cases/retrieval/read_concepts.py, app/core/use_cases/retrieval/deterministic_graph_recall.py]
shrink: `ReadMemoryResult.to_response_data` returns the same `{"pack": ...}` as `.data`; `ok_envelope` falls back to `.data` with identical output and 60+ test lines already read `.data`. Drop `to_response_data`. [app/core/use_cases/retrieval/read/result.py]
yagni: `_deterministic_strategy_result(..., fallback_used)` has one caller passing `False`. Hard-code it. [app/core/use_cases/retrieval/build_context/execute.py]
net: -385 lines, -0 deps possible.

## Agent 3 — Persistence and schema

_Audit only; source edits are prohibited. Every symbol below was grep-checked across app/, tests/, migrations/, scripts/, pyproject.toml, onboarding_assets/, docs/. Items applied in 3054d4c are not repeated. Historical Alembic revisions stay untouched; schema removals go in one new revision (0039)._

delete: Remove the `session_transfers` table, `IEpisodesRepo.append_transfer`, `EpisodesRepo.append_transfer`, and `test_session_transfer_rows_reject_self_transfers`; the only writer is the repo method and it has zero callers (port+repo are its only two references; the test inserts rows by hand). Drop the table in a new revision 0039 and remove the `SessionTransfer` entity from core. [app/infrastructure/db/runtime/models/episodes.py, app/infrastructure/db/runtime/repos/relational/episodes_repo.py, app/core/ports/db/episode_repositories.py, tests/operations/update/execution/failure_handling/test_db_constraints.py]
delete: Remove the `graph_patches` table, `_PATCH_STATUSES`, `IConceptsRepo.create_graph_patch`, and `ConceptsRepo.create_graph_patch`; no use case calls it, the entity docstring says "reserved future", and the inner-agent prompt already forbids the table. Drop the table in revision 0039 and remove `GraphPatch`/`GraphPatchStatus` from core. [app/infrastructure/db/runtime/models/concepts.py, app/infrastructure/db/runtime/repos/relational/concepts_repo.py, app/core/ports/db/concept_repositories.py]
delete: Remove `IEvidenceRepo.resolve_evidence`, `EvidenceRepo.resolve_evidence`, `_link_view_from_row`, and `_source_from_row`; no app code reads evidence through the repo (only three tests in tests/operations/evidence call it, and they already assert against `fetch_rows(evidence_links)`). Nothing replaces it. [app/infrastructure/db/runtime/repos/relational/evidence_repo.py, app/core/ports/db/memory_repositories.py]
shrink: Collapse the eight identical single-table `_TARGET_VALIDATION_QUERIES` entries into one `SELECT 1 FROM {table} WHERE id = :target_id AND repo_id = :repo_id LIMIT 1` template driven by a `{EvidenceTargetType: table_name}` dict; keep only the memory and utility_observation specials. [app/infrastructure/db/runtime/repos/relational/evidence_repo.py]
shrink: Replace the three byte-identical `_visibility_filters` methods (keyword, semantic, read-policy repos) and the inline copy in `_visible_corpus_rows` with one module-level `visible_memory_filters(repo_id, include_global, kinds)`; also import `_is_zero_vector`/`_is_positive_score` from `semantic_retrieval_repo` instead of redefining them in `concept_retrieval_repo`. [app/infrastructure/db/runtime/repos/semantic/keyword_retrieval_repo.py, app/infrastructure/db/runtime/repos/semantic/semantic_retrieval_repo.py, app/infrastructure/db/runtime/repos/relational/read_policy_repo.py, app/infrastructure/db/runtime/repos/semantic/concept_retrieval_repo.py]
delete: Remove `IEpisodesRepo.create_episode`/`EpisodesRepo.create_episode`, `list_event_keys`, and `close_episode`; `list_event_keys` and `close_episode` have zero callers, and `create_episode` is called only by four test fixtures that all pass a `thread_id`, so `get_or_create_episode_for_thread` is a drop-in there (its insert block is a duplicate of `create_episode`). [app/infrastructure/db/runtime/repos/relational/episodes_repo.py, app/core/ports/db/episode_repositories.py]
shrink: Fold `insert_read_summary`, `insert_recall_summary`, and `insert_write_summary` (identical delete-items/delete-summary/insert/insert bodies) into one `_replace_summary(summary_table, items_table, summary, items)` helper called three times. [app/infrastructure/db/runtime/repos/relational/telemetry_repo.py]
shrink: Add `_to_episode(row)` and `_to_event(row)` mappers; the Episode constructor is duplicated in `get_episode_by_thread`/`get_episode` and the EpisodeEvent constructor three times in `get_event`/`list_recent_events`/`list_events_range`. [app/infrastructure/db/runtime/repos/relational/episodes_repo.py]
yagni: Fold `manifest_store.py` into `logical_backup.py`: its three functions have no caller outside that module, `list_backups` only forwards to `list_backup_manifests`, and `read_backup_manifest` is called once inside `list_backup_manifests`. One module, one `list_backups` with the rglob loop inline. [app/infrastructure/db/admin/backups/manifest_store.py, app/infrastructure/db/admin/backups/logical_backup.py]
yagni: Delete `init_effects.py`; both functions only rename-forward to `docker_prerequisites.ensure_docker_runtime_available` and `recover_managed_machine_config_from_docker`. Import those directly in `app/startup/runtime_admin.py` and update the three `monkeypatch.setattr(init_module, "ensure_managed_runtime_available", ...)` names in tests/operations/recovery/execution/test_init_bootstrap.py. [app/infrastructure/db/admin/provisioning/init_effects.py]
yagni: Drop never-populated schema in the same revision 0039: `episodes.title`/`episodes.objective` (both app `Episode(...)` constructions omit them; only raw test inserts set them), `association_edges.last_used_at` (no reader or writer anywhere since 0001), and `recall_source_items.output_section` plus `ck_recall_source_items_output_section` (`RecallSourceItemRecord` has no such field, so it is always NULL and the write test asserts `None`). Remove the matching `Column`s, the two `title=`/`objective=` repo mappings, and the `title`/`objective` fields on core `Episode`. [app/infrastructure/db/runtime/models/episodes.py, app/infrastructure/db/runtime/models/associations.py, app/infrastructure/db/runtime/models/telemetry.py, app/infrastructure/db/runtime/repos/relational/episodes_repo.py]
shrink: Merge the two identical PortBindings/Ports loops in `_container_host_ports` into one loop over both mappings, and import `_replace_database` from `logical_backup` instead of redefining the same 4-line function. [app/infrastructure/db/admin/provisioning/managed_local.py]
shrink: Replace the `_ = (associations, concepts, ...)` twelve-name tuple with `# noqa: F401` on the import; the tuple exists only to silence unused-import lint. [app/infrastructure/db/runtime/models/registry.py]
shrink: Return the evidence ref id (`str`) from `_upsert_ref` (select `evidence_refs.c.id` instead of the full row) and delete `_ref_from_row`; `_attach_one` uses only `.id`, and `EvidenceRef` has no other consumer, so the core entity goes too. [app/infrastructure/db/runtime/repos/relational/evidence_repo.py]
yagni: Merge `engine.py` and `session.py` into one `engine.py` exporting both helpers and drop `future=True` (SQLAlchemy 2.0.51 is installed; the flag is a no-op there). Two one-function modules become one. [app/infrastructure/db/runtime/engine.py, app/infrastructure/db/runtime/session.py]
shrink: Replace the `_lifecycle_target_table` and `_target_type_for_record` if-chains with two dict lookups (`{ConceptLifecycleTargetType: (table, mapper)}` and `{type: ConceptLifecycleTargetType}`); ten branches become two `KeyError`-guarded lookups. [app/infrastructure/db/runtime/repos/relational/concepts_repo.py]
shrink: In `_unified_evidence_to_concept_row`, drop `_CONCEPT_EVIDENCE_KIND_BY_SOURCE_KIND` (an identity map except `episode_event -> transcript`) and the redundant `values["evidence_kind"] = "transcript"` reassignment; `"transcript" if source_kind == "episode_event" else source_kind`, 1 line. [app/infrastructure/db/runtime/repos/relational/concepts_repo.py]
shrink: Replace `_normalize_admin_dsn` (6 lines) with `(admin_dsn or "").strip() or None`, and `_close_interactive_stream` (5 lines) with `if handle: handle.close()` at its single call sites. [app/infrastructure/db/admin/storage_setup.py]
shrink: Have `_resolve_instance_metadata` return `tuple[str, str]` and delete the `_ResolvedInstanceMetadata` dataclass; the two fields are read once each in `create_backup`. [app/infrastructure/db/admin/backups/logical_backup.py]
stdlib: Replace the private `_fetch_schema_revision` (own psycopg connect + cursor) with `connection.fetch_schema_revision(admin_dsn)` plus a `RuntimeError` when it returns `None`; same query, one implementation. [app/infrastructure/db/admin/backups/logical_backup.py]

Verified and retained: `migrations/_usage_view_sql.py` and `_legacy_usage_views.py` constants are all imported by at least one revision (`USAGE_PROBLEM_TOKENS_PRE_STRUCTURAL_SQL` is an internal intermediate); `env.py` offline mode is covered by tests/config/test_alembic_env.py; `_legacy_dsn_fingerprint`/`include_legacy_lookup` is a live compatibility path for stamped installs; every `queries/*.py` function has a startup or script caller; `uow.guidance` aliases a real core port with a core caller.

net: -500 lines, -0 deps possible.

## Agent 4 — Runtime and host integration

_Audit only; no source edits. Items already applied in 3054d4c (operation_command wrapper, JSON `render`, upgrade handler) are not repeated._

shrink: `CodexCliInnerAgentRunner.run`, `run_build_knowledge`, and `run_teach_knowledge` repeat the same 85-line `which`/tempdir/`subprocess.run`/timeout/nonzero-exit/parse block three times (lines 58-324). Extract one `_run_codex(request, prompt, mode, run_id)` plus shared error mapping, the shape `claude_cli._run_knowledge` already uses. [app/infrastructure/host_apps/inner_agents/codex_cli.py]
yagni: Delete the host-hook module-name plumbing: `app/entrypoints/host_hooks/` (3 shims), `app/startup/host_hooks.py`, and the `session_start_module`/`statusline_module`/`claude_session_start_module`/`cursor_statusline_module` parameters with their `**({} if x is None else {...})` dances in `assets/service.py`, `assets/claude.py`, `assets/cursor.py`, and `startup/admin.py`. Always install the infrastructure modules' existing `__main__` (`identity.claude_runtime`, `identity.cursor_statusline`). Evidence: `shellbrain init` already installs that path because `runtime_admin` passes raw `install_host_assets` with no module names (tests assert the infra command string at `test_claude_hook_install.py:132`), so today `init` and `admin install-host-assets` write two different hook commands. Update the two allowlists in `tests/config/test_architecture_boundaries.py`. [app/entrypoints/host_hooks, app/startup/host_hooks.py, app/infrastructure/host_apps/assets]
yagni: Delete the 63-line `TelemetryWriteRepository`/`TelemetryUnitOfWork` Protocols; the only implementation is `PostgresUnitOfWork.telemetry` and the only consumer is three `uow:` annotations in `recorder.py`. Annotate with `Any`. [app/infrastructure/telemetry/storage_protocols.py]
shrink: Replace the 11 near-identical `if command == ...` branches in `_dispatch_operation_command` (lines 222-438) with one route table `{command: (prepare_fn, run_fn, extra_kwargs)}`; only `defaults=`, embedding args, `requested_limit`, and `capture_repo_root` vary. [app/entrypoints/cli/runner.py]
delete: Remove the dead startup config helpers with no callers anywhere in app/, tests/, or scripts/: `get_internal_agents_settings` (internal_agents.py:89), `get_threshold_settings` (thresholds.py:27), `resolve_read_limit` and `resolve_read_quotas` (read_policy.py:163-176), `get_create_policy_settings` and `validate_create_policy_settings` (create_policy.py:35,47), `validate_update_policy_settings` (update_policy.py:34), and `get_update_policy_settings` (update_policy.py:28; its single test `tests/config/test_update_policy_usage.py` can assert on `get_typed_update_policy_settings().to_dict()`). Also delete the never-populated `create_policy_errors`/`update_policy_errors` fields (startup never passes them; the handler branches in memories/add.py:57 and memories/update.py:62 are dead). [app/startup/read_policy.py, app/startup/create_policy.py, app/startup/update_policy.py, app/startup/thresholds.py, app/startup/internal_agents.py, app/startup/operation_dependencies.py, app/entrypoints/cli/handlers/dependencies.py]
shrink: Dedupe the transcript adapters: `_build_event` x3 (differs only by `host_app` and `raw_ref` prefix), `_normalized_tool_status` x3, `_fallback_key` x2, and `codex._usage_key` == `model_usage._fallback_usage_key`. One shared `build_event(host_app, raw_ref, ...)` and one status/key helper module. [app/infrastructure/host_apps/transcripts/claude_code.py, codex.py, cursor.py, model_usage.py]
shrink: Dedupe the token/provider coercers: `_provider_from_model_id` (cursor.py:798, cursor_statusline.py:224) == `model_usage._infer_provider`; `_coerce_non_negative_int` (cursor_statusline.py, model_usage.py) and `claude_code._coerce_int`; `_clean_string` x2; Cursor-home resolution in `cursor_statusline._cursor_home`, `model_usage._cursor_statusline_sidecar_path`, and `assets/paths.default_cursor_home`. Keep one of each. [app/infrastructure/host_apps/identity/cursor_statusline.py, app/infrastructure/host_apps/transcripts/model_usage.py, app/infrastructure/host_apps/transcripts/cursor.py]
shrink: The three host asset adapters repeat the skill-install loop, the legacy-removal loop, and the `{"path","installed","managed"}` inspect dict; `PRIMARY_*_SKILL_NAME = "shellbrain"` and `LEGACY_*_SKILL_NAMES = ("shellbrain-session-start",)` are declared three times. Add `install_skill_trees(...)` and `skill_report(...)` to `managed_tree.py` and one shared constant pair. [app/infrastructure/host_apps/assets/claude.py, codex.py, cursor.py]
delete: Remove the unused `error_responses.py` module (no importer), `error_envelope.from_exception` (never called), the `error_envelope` class-with-`__new__` and its ignored `stage` kwarg, and the duplicate `error_response` wrapper. One plain `error_response(errors) -> dict` remains. [app/entrypoints/cli/error_responses.py, app/entrypoints/cli/handlers/result_envelopes.py]
delete: Remove `selection_summary_from_runtime_context` (selection.py:76-106); it has no caller and `TelemetrySink._selection_summary_from_runtime_context` is the live copy. Cascade: the `summarize_runtime_selection` field on the handler Protocol (dependencies.py:69) and the startup dataclass (operation_dependencies.py:87,118) is then unread; `TelemetrySink` receives it directly. [app/entrypoints/cli/handlers/internal_agent/episodes/selection.py, app/entrypoints/cli/handlers/dependencies.py, app/startup/operation_dependencies.py]
shrink: Consolidate the duplicated small utilities: `_parse_iso` (handlers/session_state.py, local_state/session_state_file_store.py), `_is_process_running` (lock_file.py) vs `_pid_exists` (init_lock.py), `_remove_path` (lock_file.py) vs `remove_existing_path` (managed_tree.py), `_required_str`/`_optional_str` (machine_config_store.py, repo_registration_store.py). [app/infrastructure/local_state, app/infrastructure/process/episode_sync/lock_file.py]
shrink: The four hydrators `hydrate_concept_add_payload`, `hydrate_concept_update_payload`, `hydrate_concept_show_payload`, `hydrate_scenario_record_payload` are the same three lines (`setdefault("repo_id", ...)`). One `hydrate_repo_id`. Also drop the tautology `error_stage="schema_validation" if errors else "schema_validation"` (x3 in concepts.py; it is the dataclass default). [app/entrypoints/cli/request_parsing/hydration.py, app/entrypoints/cli/request_parsing/concepts.py]
shrink: Duplicated test helper `_stub_read_pipeline` (verbatim in test_operation_invocation_record_writes.py:391 and test_read_summary_record_writes.py:236). Move to `tests/operations/_shared`. [tests/operations/telemetry/execution/operation_invocations/test_operation_invocation_record_writes.py, tests/operations/telemetry/execution/read_summaries/test_read_summary_record_writes.py]
yagni: Delete `app/startup/use_cases.py`; `get_uow_factory` returns `repos.get_uow`, `get_embedding_provider_factory` returns `embeddings.get_embedding_provider`, and `get_embedding_model` forwards to `embeddings.get_embedding_model_name`. Wire those names directly in `startup/cli.py` and the four startup importers; retarget the four `monkeypatch.setattr("app.startup.use_cases.get_uow_factory", ...)` seams in `test_operation_invocation_record_writes.py` to `app.startup.repos.get_uow`. [app/startup/use_cases.py]
shrink: `read_policy.py` builds `ReadPolicySettings`, calls `.to_dict()`, then rebuilds it in `_coerce_read_policy_settings` for every consumer. Call `get_read_policy_settings().hydration_defaults()` / `.retrieval_defaults()` / `.resolve_payload_defaults()` directly and delete `get_read_settings` plus the coercer; retarget the two `get_read_settings` monkeypatches in `tests/config/test_read_policy_usage.py` to `get_read_policy_settings`. [app/startup/read_policy.py]
delete: Remove `runtime_admin._determine_outcome` (lines 312-328); it is a startup copy of `initialize_runtime._determine_outcome` and nothing calls it. [app/startup/runtime_admin.py]
delete: Remove `SessionStateManager.clear_for_caller`, `garbage_collect`, and `GC_EXPIRY`; `admin session-state clear|gc` goes through `startup/admin.py` straight to `FileSessionStateStore`. [app/entrypoints/cli/handlers/session_state.py]
shrink: `resolver._search_roots_for_host` duplicates `session_selection._search_roots_for_host`; its extra `host_app not in SUPPORTED_HOSTS` guard is already what `default_search_roots` returns (`[repo_root]`). Import the shared one. [app/infrastructure/host_apps/identity/resolver.py]
shrink: `get_embedding_model_name` and `get_embedding_provider` repeat the same provider/model validation and the single-provider `if provider == "sentence_transformers"` switch. Have the provider factory call `get_embedding_model_name()`. [app/startup/embeddings.py]
yagni: `ensure_telemetry_context` declares its own `OperationContextDependencies` Protocol, but every caller passes an `OperationDependencies`, which already carries those three attributes. Annotate with `OperationDependencies` and delete the Protocol. [app/entrypoints/cli/handlers/dependencies.py]
shrink: `create_policy.py` and `update_policy.py` repeat `_SUPPORTED_GATES` and the same four gate checks. One `_parse_gates(policy, prefix)`. [app/startup/create_policy.py, app/startup/update_policy.py]
shrink: `PollerLockHandle.release` is the only caller of `release_poller_lock`, and `PollerLockInspection.active` is only read by `blocks_acquisition`; inline both. [app/infrastructure/process/episode_sync/lock_file.py]
shrink: `installed_shellbrain_version` (managed_tree.py:99) is byte-identical to `db/admin/migrations._installed_shellbrain_version`; keep one in `app/infrastructure/system/`. The parser copy stays because entrypoints may not import infrastructure. [app/infrastructure/host_apps/assets/managed_tree.py]
yagni: `_build_external_machine_config` wraps `external_runtime.build_fresh_machine_config` in a `try/except TypeError` that retries positionally; the real function requires `embeddings=` and the fallback only serves the one-arg stub at `tests/operations/recovery/execution/test_init_bootstrap.py:649`. Make the stub accept `**kwargs`, call directly. [app/startup/runtime_admin.py]
shrink: `ok_envelope` has five shape branches; every caller passes a dict or a result exposing `to_response_data`/`data`, so the `BaseModel` and `{"result": ...}` branches are unreachable. [app/entrypoints/cli/handlers/result_envelopes.py]
shrink: `poller._write_pid_artifact` only forwards to `write_poller_pid_artifact` (tests already patch the module-level name); `_run_build_knowledge_plan_best_effort` re-checks `run_build_knowledge is None` and a `plan is None` that the generator never yields. [app/infrastructure/process/episode_sync/poller.py]
shrink: `repo_context._load_registration_for_root` only forwards to `load_repo_registration_for_target`. Call it directly. [app/startup/repo_context.py]
shrink: `startup/cli.py` wraps `managed_backup_kwargs` in `lambda _machine_config, _machine_error: ...` and the handler calls it as `(None, None)`. Pass the function; type it `Callable[[], dict]`. [app/startup/cli.py, app/entrypoints/cli/handlers/human/admin.py, app/entrypoints/cli/handlers/human/admin_dependencies.py]
delete: Remove the `app/startup/retrieval.py` re-export shim; its one importer (`tests/operations/read/execution/context_pack/test_context_pack_config_defaults.py`) can import `get_retrieval_defaults` from `app.startup.read_policy`. [app/startup/retrieval.py]
stdlib: `sys_platform_linux()` is `os.name != "nt" and "linux" in os.sys.platform`. `sys.platform.startswith("linux")`, inline at its one call site. [app/infrastructure/host_apps/transcripts/cursor.py]

Verified live (not findings): every key in `app/settings/defaults/*.yaml` and `internal-agents/defaults.yaml` is read by code; every CLI subcommand and flag in `parser/builder.py` has a handler route; `CliRuntime`/`AdminCommandDependencies` Protocol-plus-dataclass pairs are the entrypoint/startup boundary enforced by `tests/config/test_architecture_boundaries.py` and are retained.

net: -850 lines, -0 deps possible.

## Agent 5 — Project delivery

delete: Remove both completed refactor plans. `brief.sources` is gone from every recall producer and the wiki slice is gone (drop migration `20260815_0038`, version now 0.1.56 > planned 0.1.51); nothing in the site, sitemap, README, or tests links them. [docs/superpowers/plans/2026-07-14-remove-recall-sources.md, docs/superpowers/plans/2026-08-15-remove-amp-and-wiki.md]
delete: Remove the one-shot SVG generator. Its output `docs/assets/shellbrain-memory-ontology-constellation.svg` is committed; the only repo reference to the script is the comment inside that SVG, and no CI, test, README, or doc runs it. [scripts/generate_memory_ontology_constellation.py]
shrink: Collapse `docs/upgrade` into `docs/install`. `diff` shows 407 vs 407 lines with ~30 differing (message text, `--no-cache-dir`, a `bootstrap_state` check); make install read one `SHELLBRAIN_UPGRADE` flag and turn `upgrade` into a 3-line wrapper that fetches install with the flag set. Existing `test_install_script.py`/`test_onboarding_assets.py` runs keep exercising both paths. [docs/upgrade, docs/install]
delete: Remove the unreferenced test-host status script. `grep -rn storage_status` across the repo returns nothing outside the file. [scripts/storage_status]
delete: Remove the whole Cursor asset tree and have `cursor.py` install from `claude/skills/<name>`. `cursor/skills/shellbrain/SKILL.md` is byte-identical to Claude's; `cursor/skills/shellbrain-usage-review/SKILL.md` differs from Claude's only by the 5-line frontmatter Claude's copy should carry anyway (and is byte-identical to Codex's). Update the one path in `test_packaged_cursor_skill_should_include_the_required_skill_file`. [onboarding_assets/cursor/]
shrink: Link `page.css` from `docs/index.html` and keep only home-specific rules. After whitespace normalization 205 of the 288 non-blank `page.css` lines also appear verbatim inside the 836-line inline `<style>` (36 shared selectors: `.button`, `.nav`, `.hero`, `.footer`, `.install-command`, `:root` tokens...). [docs/index.html, docs/page.css]
delete: Remove tombstone tests that assert long-deleted paths and commands stay deleted (`*_is_gone`, `test_followup_refactor_removed_old_layer_paths`, `test_refactor_old_layer_directories_are_gone`, `test_no_code_imports_removed_architecture_paths`, `test_core_use_case_apply_files_are_gone`, `test_deleted_agent_operations_tree_is_not_a_guardrail_target`, `test_runtime_code_does_not_reference_retired_compatibility_tables`, `test_metrics_command_should_not_be_registered`, `test_admin_should_not_expose_concept_embedding_backfill_commands`). Git history is the record; the live boundary tests already cover current layout. [tests/config/test_architecture_boundaries.py, tests/config/test_cli_surface.py]
delete: Remove the Docker image definition and its ignore file. No `docker build`, CI job, test, README, or doc builds it (only a one-line mention in external-quickstart.md), and the image `COPY`s neither `migrations/` nor `onboarding_assets/` that pyproject packages, so `shellbrain admin migrate` cannot run inside it. `.dockerignore` is 101 lines of patterns for paths the Dockerfile never copies. [Dockerfile, .dockerignore]
delete: Remove `page.js` and the inline copy script in `index.html`; give the `<code>` elements `id="install-cmd"` and the buttons `data-copy-source="#install-cmd"`. `site.js` already ships that handler and the `install_command_copy` GA event name, and no element uses it today. [docs/page.js, docs/index.html, docs/humans/index.html]
delete: Remove the `data-copy-page` handler (no element in any page carries `data-copy-page`) and the favicon injector (all four pages declare `<link rel="icon">`, so it always early-returns). [docs/site.js]
delete: Remove `codex/AGENTS.md`; it is byte-identical to `claude/CLAUDE.md`. Load one packaged file for both managed-markdown blocks and drop the second path from `test_packaged_startup_guidance_assets_should_exist_for_codex_and_claude`. [onboarding_assets/codex/AGENTS.md]
delete: Remove the dev compose file and env template. The managed runtime provisions with `docker run` (`provisioning/managed_local.py`); `grep -rn "docker-compose.yml\|env.example"` finds only the `.gitignore` exception. Drop `!.env.example` with them. [docker-compose.yml, .env.example, .gitignore]
shrink: Replace `MAJOR_CATEGORY_ORDER`/`MAJOR_CATEGORY_LABELS` with `sorted(grouped_by_major)` and `_humanize_heading(key) + " Tests"` (the order is already alphabetical, the labels already Title Case). Side effect: the report stops silently dropping the 10 operation dirs missing from the list (concepts, evidence, knowledge_builder, librarian, memories, memory_domain, recall, retrieval, scenarios, snapshots). [tests/visualization/report_builder.py]
yagni: Drop the `docker` and `persistence` markers. No `-m` selection, `get_closest_marker`, or `iter_markers` reads them anywhere; they are always applied as a pair on the same six files, which skip via fixtures. Remove the two registrations and the twelve decorators. [pytest.ini]
yagni: Drop the ubuntu leg of `packaging-smoke` (the `tests` job already runs `test_packaging_smoke.py` on ubuntu, and the tests build their own clean-room venv) and the `upload-artifact` step that ships the same file the previous step already wrote to `$GITHUB_STEP_SUMMARY`. [.github/workflows/ci.yml]
shrink: Replace the seven pinned dependency lines with `-e .` plus `pytest>=8.0,<9.0`; pyproject is the single source of versions. Add `pyproject.toml` to the two `hashFiles` cache keys. [requirements.txt]
yagni: Remove the `os.name == "nt"` branches in `venv_python`/`venv_shellbrain`; the installer is bash-only and CI runs ubuntu/macos. Return the posix paths. [tests/_shared/packaging_smoke_helpers.py]
shrink: Drop the post-build pytest run (line 66) and the `pytest`/`psycopg` install it needs (line 24); CI already ran both files on the same commit. Keep the wheel-contents check. [.github/workflows/release.yml]
delete: Remove two unreferenced screenshots; no page, README, onboarding asset, or test names them. [docs/assets/codex_usage.png, docs/assets/claude_usage.png]

net: -2700 lines, -0 deps possible.

## Merged findings

Findings are deduplicated across agents (GraphPatch, SessionTransfer, and the unused episode port methods were reported by agents 1 and 3; the Windows venv branches by agent 5 overlap agent 4's packaging scope). Each deletion below was re-checked against imports, callers, tests, build files, configuration, and packaging before it was applied. No dependency change is supported: all seven declared production dependencies have direct production imports.

### Safe to implement now

Every line below was applied in this working tree. Ranked biggest cut first within each group.

**Core domain**

yagni: Removed the `verify_backup` and `create_backup` use cases and `BackupVerificationResult`; startup calls the `logical_backup` adapter directly. [app/core/use_cases/admin, app/core/entities/backups.py, app/startup/backup.py]
yagni: Removed `SyncEpisodeRequest.from_raw_events`; pydantic validates mappings and instances identically, so the three callers construct the request directly. [app/core/use_cases/episodes/sync_episode/request.py]
yagni: Removed `CodeDeltaContextStatus`, the constant `status` fields and guards, and the `_unavailable` one-liner; payload unchanged. [app/core/entities/snapshots.py, app/core/use_cases/snapshots/code_delta_context/execute.py]
shrink: Removed `make_side_effect`; nine call sites build `PlannedEffect(EffectType.X, params)` directly. [app/core/use_cases/memories/effect_plan.py, app/core/policies/memories/add_plan.py, update_plan.py]
delete: Deleted `event_content.py` and `to_content_dict`; one `json.dumps(event.model_dump(...))` at the single call site. [app/core/policies/episodes/event_content.py]
delete: Deleted `containment_checks.py`; the three-line guard lives in `_add_relation`. [app/core/use_cases/concepts/containment_checks.py]
delete: Removed the unreachable duplicate-slug branch in `add_concepts`; the request validator uses the same normalizer and rejects first. [app/core/use_cases/concepts/add/execute.py]
stdlib: `collections.Counter` replaces the hand-rolled counters in `_count_normalized_events`, `_evidence_counts`, and `_status_rollup`. [app/core/use_cases/episodes/sync_episode/sync_episode.py, app/core/use_cases/concepts/views.py]
stdlib: `tuple(dict.fromkeys(terms))` replaces `_unique_terms`; dropped the write-only `raw_terms`/`informative_terms` fields and the never-passed `k1`/`b` parameters. [app/core/policies/retrieval/lexical_query.py, bm25.py]
yagni: Removed `MemoryAddLinks.related_memory_ids` and its reject-everything validator; `extra="forbid"` reports the same field path. Edge: an empty list was silently accepted before and is rejected now. [app/core/use_cases/memories/add/request.py]
delete: Removed the `data` properties on `UpdateMemoryResult` and `BatchUpdateMemoryResult`; `ok_envelope` uses `to_response_data`. [app/core/use_cases/memories/update/result.py]
yagni: Removed `BackfillModelUsageRequest`; `backfill_model_usage` takes the sessions tuple. [app/core/use_cases/admin/backfill_model_usage, app/startup/model_usage_backfill.py]
shrink: `MATURE_MEMORY_KIND_VALUES` derives from `MemoryKind`; inlined `build_canonical_caller_id`. [app/core/entities/memories.py, identity.py]
delete: Removed `GuidanceDecision.setup_hint`, three unused `ErrorCode` members, and the `InvocationId`/`UtilityVote` NewTypes. [app/core/entities/guidance.py, app/core/errors.py, app/core/entities/ids.py]

**Retrieval and knowledge**

delete: Deleted `recall/execute.py` (`execute_recall_memory` forwarded unchanged to `execute_build_context`); the CLI handler imports `execute_build_context`. The recall and build_context packages are plain imports now; no cycle. [app/core/use_cases/retrieval/recall, build_context/__init__.py, app/entrypoints/cli/handlers/internal_agent/retrieval/execution.py]
shrink: `build_context/execute.py`: inlined the autonomous-fallback forwarder, used `nullcontext` for the uow-or-factory branch, hard-coded the constant `fallback_used`, and collapsed the three identical `RecallMemoryResult` constructions into one closure. [app/core/use_cases/retrieval/build_context/execute.py]
shrink: `deterministic_graph_recall.py`: one `_new_candidate` factory for the three repeated candidate dicts; five one-line delegating wrappers inlined; `synthesis_pack_from_graph_pack` is the function, not a wrapper; one shared `no_context_brief`. [app/core/use_cases/retrieval/deterministic_graph_recall.py]
shrink: `bundle_lifecycle_statuses` lives once in `ontology_semantics.py`; the lifecycle tables derive from `MemoryLifecycleStatus` (equality verified before replacing; `BUNDLE_FRESHNESS_MULTIPLIERS` differs and stays literal). [app/core/policies/retrieval/ontology_semantics.py, read_concepts.py]
delete: Removed `_run_status` in both knowledge-builder executors (the status is already the enum) and the `.data` properties on both results. [app/core/use_cases/knowledge_builder]
shrink: `_hydrate_pack_items` uses one `_DISPLAY_FIELDS` predicate; the unreachable "Incomplete hydrated memory" branch is gone. [app/core/use_cases/retrieval/context_pack_pipeline.py]
shrink: Inlined `_keyword_candidate_limit`, `_concept_keyword_candidate_limit`, `_freshness_multiplier`, `_concept_expand`; dropped the self-length `MAX_EXPAND_HANDLES` slice and the redundant `ReadMemoryResult.to_response_data`. [app/core/use_cases/retrieval/seed_retrieval.py, concept_seed_retrieval.py, read_concepts.py, read/result.py]

**Persistence and schema**

delete: Removed `GraphPatch`/`GraphPatchStatus`, `create_graph_patch`, `SessionTransfer`, `append_transfer`, `list_event_keys`, and `close_episode` from entities, ports, and repos. Table models stay (see Intentionally retained). [app/core/entities/concepts.py, episodes.py, app/core/ports/db, app/infrastructure/db/runtime/repos/relational]
shrink: One `visible_memory_filters` replaces three byte-identical `_visibility_filters` methods and an inline copy. [app/infrastructure/db/runtime/repos/memory_visibility.py, keyword_retrieval_repo.py, semantic_retrieval_repo.py, read_policy_repo.py]
shrink: `_to_episode`/`_to_event` mappers replace five duplicated constructors; `_replace_summary` replaces three identical summary writers. [app/infrastructure/db/runtime/repos/relational/episodes_repo.py, telemetry_repo.py]
shrink: Eight single-table evidence validation queries come from one template; the concept evidence-kind map is one conditional. [app/infrastructure/db/runtime/repos/relational/evidence_repo.py, concepts_repo.py]
yagni: Folded `manifest_store.py` into `logical_backup.py`; `_resolve_instance_metadata` returns a tuple; `replace_database` lives once in `connection.py`; merged the two identical port-binding loops. [app/infrastructure/db/admin/backups, admin/connection.py, admin/provisioning/managed_local.py]
shrink: Inlined `_normalize_admin_dsn`; `# noqa: F401` replaces the twelve-name registry tuple; dropped the no-op `future=True`. [app/infrastructure/db/admin/storage_setup.py, runtime/models/registry.py, runtime/engine.py]

**Runtime and host integration**

delete: Removed seven uncalled startup config helpers and the never-populated `create_policy_errors`/`update_policy_errors` fields with their dead handler branches. [app/startup/{internal_agents,thresholds,read_policy,create_policy,update_policy,operation_dependencies}.py, app/entrypoints/cli/handlers]
delete: Deleted `startup/use_cases.py`, `startup/retrieval.py`, `cli/error_responses.py`, and `provisioning/init_effects.py`; callers import the real functions. Six test seams retargeted. [app/startup, app/entrypoints/cli, app/infrastructure/db/admin/provisioning]
delete: Removed `selection_summary_from_runtime_context` and the unread `summarize_runtime_selection` dependency field; `TelemetrySink` still receives it. [app/entrypoints/cli/handlers/internal_agent/episodes/selection.py, dependencies.py, app/startup/operation_dependencies.py]
shrink: One `error_response` function replaces the `error_envelope` class, its ignored `stage`, and `from_exception`; `ok_envelope` lost two unreachable branches. [app/entrypoints/cli/handlers/result_envelopes.py]
shrink: One `hydrate_repo_id` replaces four identical hydrators; three `error_stage` tautologies dropped. [app/entrypoints/cli/request_parsing/hydration.py, concepts.py, scenarios.py]
delete: Removed `_determine_outcome` copy, the `TypeError` positional retry, `clear_for_caller`, `garbage_collect`, `GC_EXPIRY`, and `sys_platform_linux`. [app/startup/runtime_admin.py, app/entrypoints/cli/handlers/session_state.py, app/infrastructure/host_apps/transcripts/cursor.py]
shrink: `resolver` imports the shared `_search_roots_for_host`; `managed_backup_kwargs` is passed directly instead of through a two-arg lambda; `_load_registration_for_root`, `_write_pid_artifact`, `release_poller_lock`, and `PollerLockInspection.active` inlined; `get_embedding_provider` reuses `get_embedding_model_name`. [app/infrastructure/host_apps/identity/resolver.py, app/startup/cli.py, repo_context.py, embeddings.py, app/infrastructure/process/episode_sync]
yagni: `ensure_telemetry_context` takes `OperationDependencies`; the one-off Protocol is gone. [app/entrypoints/cli/handlers/dependencies.py]
shrink: One `stub_read_pipeline` test helper replaces two copies. [tests/operations/_shared/read_pipeline_stubs.py]

**Project delivery**

delete: Removed two completed refactor plans and two unreferenced screenshots. [docs/superpowers/plans, docs/assets]
delete: Removed the `data-copy-page` handler (no element uses it) and the favicon injector (all four pages declare an icon). [docs/site.js]
yagni: Removed the Windows venv-path branches from the bash-only packaging helpers. [tests/_shared/packaging_smoke_helpers.py]

### Needs follow-up evidence

These reduce code but change a test seam, a public payload, a schema, or a cross-module contract, so they need a targeted check or a decision before they land.

shrink: Fold the twelve provider run fields into one `RUN_RESULT_FIELDS` spread and share the stale-run timeout loop between the two knowledge-builder executors. Behavior is identical but the change spans two executors and their tests. [app/core/use_cases/knowledge_builder]
yagni: Hold a `BuildKnowledgeResult` inside `TeachKnowledgeResult` instead of re-declaring 18 fields. About eight test sites move to `result.build.*`. [app/core/use_cases/knowledge_builder/teach_knowledge/result.py]
shrink: Keep one `resolve_query_embedding` and drop the `vector_search` parameter from `retrieve_seeds` and `build_context_pack`. Two direct test callers change. [app/core/use_cases/retrieval/seed_retrieval.py, read/execute.py, deterministic_graph_recall.py, context_pack_pipeline.py]
yagni: Replace the read package `__getattr__` and `_build_context_pack` re-lookup with a module import; retarget the two monkeypatching tests. Retained last pass as a deliberate seam; the seam survives on the pipeline module. [app/core/use_cases/retrieval/read/__init__.py, read/execute.py]
yagni: Make `build_context_settings` required in `execute_build_context`; production always injects it. Three defaulting tests need a settings helper. [app/core/use_cases/retrieval/build_context/execute.py]
shrink: One shared `iso_timestamp` for the four identical `_iso` helpers. [app/core/use_cases/retrieval/context_pack_pipeline.py, read_concepts.py, deterministic_graph_recall.py, concepts/views.py]
shrink: Consolidate the nine identical `_StrictModel` bases and the duplicated string normalizers into one core request-base module. [app/core/use_cases/*/request.py, app/core/ports/host_apps/inner_agents.py]
yagni: Remove the eight constant-injection fields from `InitializeRuntimePorts` and `DiagnoseRuntimePorts`; core can import the `BOOTSTRAP_STATE_*`/`RUNTIME_MODE_*` constants directly. Startup wiring and test fixtures change. [app/core/use_cases/admin/initialize_runtime.py, diagnose_runtime.py]
shrink: Collapse the five evidence builders and the four row-filter blocks in the analytics use cases. Behavior-equivalence needs a before/after fixture diff. [app/core/use_cases/admin/generate_analytics_report.py, app/core/use_cases/metrics/analyze_agent_behavior.py]
yagni: Remove `IEvidenceRepo.resolve_evidence` and its two row mappers; only three evidence tests call it, and they already assert on raw rows. Tests need rewriting. [app/infrastructure/db/runtime/repos/relational/evidence_repo.py, app/core/ports/db/memory_repositories.py]
yagni: Remove `IEpisodesRepo.create_episode`; four test fixtures switch to `get_or_create_episode_for_thread`. [app/infrastructure/db/runtime/repos/relational/episodes_repo.py]
shrink: Dict lookups for `_lifecycle_target_table` and `_target_type_for_record`; the unknown-type error changes from `ValueError` to `KeyError` unless guarded. [app/infrastructure/db/runtime/repos/relational/concepts_repo.py]
shrink: Return the evidence ref id from `_upsert_ref` and delete `_ref_from_row` plus the `EvidenceRef` entity. Crosses the core entity boundary. [app/infrastructure/db/runtime/repos/relational/evidence_repo.py]
yagni: Delete the host-hook module-name plumbing (`app/entrypoints/host_hooks`, `app/startup/host_hooks.py`, four `*_module` parameters). Today `init` and `admin install-host-assets` write different hook commands; deleting picks the `init` path. Needs a product decision on which command is canonical. [app/entrypoints/host_hooks, app/startup/host_hooks.py, app/infrastructure/host_apps/assets]
shrink: Route table for the eleven `_dispatch_operation_command` branches; codex_cli triple `subprocess.run` block extraction; transcript adapter, coercer, and asset adapter dedupes. Each is a pure refactor of 100 to 250 lines and deserves its own review. [app/entrypoints/cli/runner.py, app/infrastructure/host_apps/inner_agents/codex_cli.py, app/infrastructure/host_apps/transcripts, app/infrastructure/host_apps/assets]
shrink: Consolidate `_parse_iso`, `_is_process_running`/`_pid_exists`, `_remove_path`/`remove_existing_path`, `_required_str`/`_optional_str`, `installed_shellbrain_version`, and the `_SUPPORTED_GATES` checks. Small individually; batch them. [app/infrastructure/local_state, app/infrastructure/process, app/startup/create_policy.py, update_policy.py]
shrink: Call `get_read_policy_settings()` accessors directly and delete `get_read_settings` plus the dict round-trip coercer; two tests in tests/config retarget. [app/startup/read_policy.py]
shrink: Collapse `docs/upgrade` into `docs/install` behind one flag; link `page.css` from `docs/index.html`; replace `page.js` with the `site.js` copy handler. Static-site changes need a browser check. [docs/upgrade, docs/install, docs/index.html, docs/page.js]
delete: Remove the Cursor asset tree and `codex/AGENTS.md` and install from the Claude copies. Packaging and install-path behavior change; two onboarding tests retarget. [onboarding_assets/cursor, onboarding_assets/codex/AGENTS.md]
shrink: Replace `MAJOR_CATEGORY_ORDER`/`MAJOR_CATEGORY_LABELS` with sorted keys and humanized labels. The report would start listing ten categories it drops today, which is a visible output change. [tests/visualization/report_builder.py]

### Intentionally retained

Items an implementation pass verified and then kept:

`CreateMemoryResult.data` stays; eight DB-backed create tests read `result.data["memory_id"]`.
`MachineConfig.machine_instance_id` stays; about twenty callers use it, so the audit line was wrong.
`validate_evidence_visibility` keeps its own lookup. Delegating to `require_visible_memory` changes the error message and field path, which is a payload change, so the edit was reverted.
`_fetch_schema_revision` in logical_backup stays; the shared `connection.fetch_schema_revision` swallows driver errors and three backup tests patch the private name.
`_is_zero_vector`/`_is_positive_score` stay duplicated; the two copies differ on NaN handling.
`_PATCH_STATUSES` stays; it feeds the check constraint on the retained `graph_patches` table model.

`_close_interactive_stream` in storage_setup stays; a recovery test monkeypatches it.
`get_update_policy_settings` stays; tests/config/test_update_policy_usage.py imports it, and the test assertion is the only consumer.
`ISessionStateStore.list` stays; the file store's garbage collection uses it.
The `rationale` fields on memory add/update request bodies stay. The models use `extra="forbid"`, so removing the fields would reject payloads that agents send today. That is a trust-boundary change, not dead code.
The `TelemetryWriteRepository`/`TelemetryUnitOfWork` Protocols stay. Replacing them with `Any` removes typing at the infrastructure seam, not code that matters.
`engine.py` and `session.py` stay separate. Merging saves one file and changes import paths.
The `graph_patches` and `session_transfers` tables, `episodes.title`/`objective`, `association_edges.last_used_at`, and `recall_source_items.output_section` stay in the schema. Dropping them needs a new Alembic revision and a decision on existing installs; the dead Python around the first two was removed.
Scripts `audit-attestation`, `run_tests`, `migrate_local_postgres_to_shellbrain`, and `agent_behavior_analysis.py` have live consumers.
All seven production dependencies have direct imports.
`docker-compose.yml` stays; six persistence tests and the legacy migration script start the database through the default compose file.
Enum members that only appear as database CHECK vocabulary define accepted input and stay.

### Decisions for the repository owner

Resolved on 2026-09-01 after review. Applied:

delete: `scripts/generate_memory_ontology_constellation.py`; its SVG output stays committed and the home page still uses it. [scripts]
delete: `scripts/storage_status`; nothing referenced it. [scripts]
delete: `Dockerfile`, `.dockerignore`, `.env.example`, and the `!.env.example` ignore rule. The managed database runs through `docker run`/`docker inspect` in `managed_local.py`, and the compose defaults are inline. `docker-compose.yml` was deleted and then restored: six Docker persistence tests and `scripts/migrate_local_postgres_to_shellbrain` run `docker compose up -d db` against the default file name, which the audit grep for the literal filename missed. `docker-compose.test.yml` is untouched. [repository root]
delete: Fourteen tombstone tests that only asserted deleted paths and commands stay deleted; no helper became orphaned. [tests/config/test_architecture_boundaries.py, test_cli_surface.py]
yagni: `docker` and `persistence` pytest markers and their twelve decorators; no `-m` selection existed and the tests skip through fixtures. [pytest.ini, tests/operations/persistence, tests/operations/telemetry/execution/persistence]
yagni: CI: `packaging-smoke` runs on macOS only (the `tests` job already runs the packaging tests on ubuntu); the duplicate report artifact upload is gone; the release workflow no longer installs pytest and psycopg to re-run two tests CI already ran. [.github/workflows/ci.yml, release.yml]
shrink: `requirements.txt` is now `-e .` plus pytest; pyproject is the single version source and the two model-cache keys hash `pyproject.toml`. [requirements.txt, .github/workflows]
delete: Alembic revision `20260901_0039` drops `session_transfers`, `graph_patches`, `episodes.title`, `episodes.objective`, `association_edges.last_used_at`, and `recall_source_items.output_section`; the SQLAlchemy models, the core `Episode` fields, the repo mappings, the prompt sentence about `graph_patches`, and the raw test inserts are updated. Downgrade recreates everything empty. `CURRENT_ALEMBIC_HEAD` moved to the new revision in two tests. [migrations/versions/20260901_0039_drop_unused_schema.py]

Still open:

yagni: Host-hook module plumbing. `shellbrain init` (what the installer and upgrader run) writes hook commands that call `app.infrastructure.host_apps.identity.claude_runtime` and `cursor_statusline`; `admin install-host-assets` writes commands that call the `app.entrypoints.host_hooks.*` shims, which forward to the same functions. Recommendation: delete the shims, `app/startup/host_hooks.py`, and the four `*_module` parameters so both commands write the infrastructure command. Only a user who ran `admin install-host-assets` by hand and then upgrades without `shellbrain init` would hold a stale hook command, and the documented upgrade path runs `init`. Not applied; awaiting a go. [app/entrypoints/host_hooks, app/startup/host_hooks.py, app/infrastructure/host_apps/assets]

## Implementation and validation

Implemented every item under Safe to implement now with four parallel implementation agents on disjoint file sets, plus the delivery-scope items directly. Nothing is committed.

Source delta, excluding this report: 110 tracked files changed, 484 lines added, 2,561 removed, plus two new files (98 lines). Net about 1,980 lines removed. Files deleted: `recall/execute.py`, `verify_backup.py`, `create_backup.py`, `event_content.py`, `containment_checks.py`, `manifest_store.py`, `init_effects.py`, `error_responses.py`, `startup/use_cases.py`, `startup/retrieval.py`, two docs plans, two screenshots. Files added: `repos/memory_visibility.py`, `tests/operations/_shared/read_pipeline_stubs.py`. No dependency changed; all seven production dependencies have direct imports.

Behavior notes worth knowing:

- `related_memory_ids: []` on memory add was silently accepted and is now rejected like any other value of that unsupported key.
- `bundle_lifecycle_statuses` kept the claims-first iteration order; every consumer is order-insensitive.
- One applied item was reverted: delegating `validate_evidence_visibility` to `require_visible_memory` changed an error message and field path.

Validation, all against the repository `.venv` (Python 3.11.14):

- Baseline before changes: `scripts/run_tests` passed, 745 tests.
- After changes: `scripts/run_tests` passed, 745 tests in 183 seconds (Docker-backed PostgreSQL, real embeddings).
- `python -m compileall -q app tests migrations` clean; `import app.entrypoints.cli.main` clean, no import cycles after the package exports became plain imports.
- `uv build` produced the wheel and sdist; the wheel contains `memory_visibility.py` and none of the deleted modules.
- `git diff --check` clean; a repository-wide grep for every deleted symbol and path found no stale references.
- No `tests/config` allowlist needed an edit; all four implementation agents grepped for their deleted paths.
- Ruff is not installed in the environment; no linter or type checker ran.

Complexity: `_hydrate_pack_items`, `ok_envelope`, `_dispatch` hydration, `_run_build_knowledge_plan_best_effort`, `get_embedding_provider`, `_lifecycle` evidence queries, and `_count_normalized_events` each lost one to four branches with identical outputs. The read-recall path lost one call layer and two lazy module dispatchers.

Deferred: everything under Needs follow-up evidence and Decisions for the repository owner.

### Second round: decision items

After the owner resolved the decision list, the applied items above were validated again:

- `scripts/run_tests` passed, 730 tests (745 minus the fourteen tombstone tests and the session-transfer constraint test), with Alembic upgraded through `20260901_0039` on a fresh test database.
- `python -m compileall -q app tests migrations` clean; `ScriptDirectory.get_heads()` reports the single head `20260901_0039`.
- A first rerun failed six Docker persistence tests with "no configuration file provided" because `docker-compose.yml` had been deleted; the file was restored and the rerun passed.
- The `ck_recall_source_items_output_section` name in the model never existed in the database (revision 0016 declared the check inline), so the migration drops the column and lets PostgreSQL drop the inline check with it.

Combined delta across both rounds, excluding this report: 139 tracked files changed, 492 lines added, 3,583 removed, plus 169 lines in three new files. Net about 2,920 lines removed. Dependencies unchanged.
