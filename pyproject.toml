[build-system]
requires = ["setuptools>=69.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "shellbrain"
version = "0.1.68"
description = "Repo-scoped Shellbrain CLI with explicit evidence-backed writes."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "SQLAlchemy>=2.0,<3.0",
  "alembic>=1.13,<2.0",
  "pydantic>=2.7,<3.0",
  "PyYAML>=6.0,<7.0",
  "psycopg[binary]>=3.1,<4.0",
  "pgvector>=0.3,<1.0",
  "onnxruntime>=1.20,<2.0",
  "tokenizers>=0.20,<1.0",
  "numpy>=1.26,<3.0",
  "huggingface-hub>=0.26,<2.0",
]

[project.scripts]
shellbrain = "app.entrypoints.cli.main:main"
_shellbrain-bootstrap = "app.entrypoints.bootstrap:main"

[tool.setuptools.packages.find]
include = ["app*", "migrations*", "onboarding_assets*"]

[tool.setuptools.package-data]
migrations = [
  "env.py",
  "versions/*.py",
]
onboarding_assets = [
  "**/*.md",
  "**/*.yaml",
  "**/*.png",
  "**/*.svg",
]
